# Experiments

These are the original exploratory scripts that started the project.

They are preserved as lab notes, so some files may:

- use absolute paths from the original machine,
- run examples directly at the bottom of the file,
- contain partially completed ideas.

For reusable code and training, use `src/dit_lab/` and `scripts/train_image.py`.
